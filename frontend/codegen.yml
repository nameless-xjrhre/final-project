schema: http://localhost:4000/graphql
documents: "./src/**/*.tsx"
generates:
  src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
    config:
      withHooks: true
